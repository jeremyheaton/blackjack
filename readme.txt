To run the program, open up a windows command prompt and naviate to the root directory 
of the project.

From there run the command 
java -jar target\blackjac-0.0.1-SNAPSHOT.jar

To run all the tests
mvn test

To do a new build run
mvn clean install